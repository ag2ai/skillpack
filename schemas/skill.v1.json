{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://skillpack.dev/schemas/skill.v1.json",
  "title": "SkillPack skill manifest (v1)",
  "description": "The package manifest for an AI-agent skill. Behavioral-package metadata: identity + runtime/model compatibility + declared permissions + I/O contract + eval/safety status + fork lineage + provenance. Serializable as skill.yaml or skill.json; this schema validates the parsed object.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "version", "maintainers", "quality"],
  "properties": {
    "schema": { "type": "string", "description": "URL of this schema, e.g. https://skillpack.dev/schemas/skill.v1.json" },
    "name": {
      "type": "string",
      "pattern": "^@[a-z0-9][a-z0-9-]*\\/[a-z0-9][a-z0-9-]*$",
      "description": "Namespaced id: @scope/name. Scope encodes trust/ownership (@core project-maintained, @community unverified, @company private, @user personal). Verification is a status (quality.safety_review), not a reserved scope."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(-[0-9A-Za-z.-]+)?$",
      "description": "SemVer. MAJOR=breaking behavior/interface; MINOR=backward-compatible feature; PATCH=fix/wording/eval."
    },
    "display_name": { "type": "string" },
    "description": { "type": "string" },
    "license": { "type": "string", "description": "SPDX id, e.g. MIT." },
    "maintainers": { "type": "array", "items": { "type": "string" }, "minItems": 1 },

    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "description": "Compatibility constraints. A resolver refuses to install a skill whose ranges exclude the target.",
      "properties": {
        "agent": { "type": "string", "description": "SemVer range of the agent runtime, e.g. '>=0.9 <2.0'." },
        "skillpack": { "type": "string", "description": "SemVer range of the SkillPack format/CLI." },
        "models": { "type": "array", "items": { "type": "string" }, "description": "Model ids the skill is validated against, e.g. 'claude-opus-4-8', 'gpt-5.5-pro'." }
      }
    },
    "entrypoint": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "instructions": { "type": "string", "description": "Relative path to the behavior spec, e.g. 'SKILL.md'." } }
    },

    "dependencies": { "type": "object", "additionalProperties": { "type": "string" }, "description": "{@scope/name: semver-range}" },
    "optional_dependencies": { "type": "object", "additionalProperties": { "type": "string" } },

    "permissions": {
      "type": "object",
      "additionalProperties": false,
      "description": "Declared, ENFORCED capabilities (lint cross-checks against actual code).",
      "properties": {
        "network": { "type": "boolean" },
        "filesystem": { "type": "string", "enum": ["none", "read", "read-write"] },
        "secrets": { "oneOf": [ { "type": "string", "enum": ["none"] }, { "type": "array", "items": { "type": "string" } } ] },
        "tools": { "type": "array", "items": { "type": "string" }, "description": "Tool/connector ids the skill may call, e.g. 'git.diff.read'." }
      }
    },

    "inputs": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "type"],
        "properties": {
          "name": { "type": "string" },
          "type": { "type": "string" },
          "required": { "type": "boolean" }
        }
      }
    },
    "outputs": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "type"],
        "properties": { "name": { "type": "string" }, "type": { "type": "string" } }
      }
    },

    "quality": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stability"],
      "description": "Trust + maturity signals surfaced by the registry.",
      "properties": {
        "stability": { "type": "string", "enum": ["experimental", "stable", "deprecated"] },
        "eval_status": { "type": "string", "enum": ["none", "failing", "passing"] },
        "eval_suite": { "type": "string", "description": "Relative path to the eval suite, e.g. 'evals/'." },
        "safety_review": { "type": "string", "enum": ["none", "pending", "verified", "unsafe"] }
      }
    },

    "lineage": {
      "type": "object",
      "additionalProperties": false,
      "description": "Keeps forks trackable and promotion decisions auditable. `parent` + `evolution` form the self-evolution audit trail — see docs/lineage-audit-trail.md.",
      "properties": {
        "forked_from": { "type": ["string", "null"], "description": "@scope/name@version this was forked from (origin)." },
        "parent": { "type": "string", "description": "@scope/name@version this version evolved from (the immediate prior version)." },
        "upstream_intent": { "type": "string", "enum": ["candidate-contribution", "private-customization"] },
        "replaces": { "type": "array", "items": { "type": "string" } },
        "variants": { "type": "array", "items": { "type": "string" }, "description": "Official variants of this skill, e.g. '@core/code-review-security'." },
        "evolution": {
          "type": "object",
          "additionalProperties": false,
          "description": "How THIS version came to be — the audit-trail entry. Chained to the parent by `parent_digest`.",
          "properties": {
            "author": { "type": "string", "description": "Who/what authored it: 'human:<handle>' or 'agent:<agent-id>' (autonomous)." },
            "policy": { "type": "string", "description": "The policy an agent acted under (agent authors only), e.g. 'auto-eval-promote-v1'." },
            "timestamp": { "type": "string", "description": "ISO-8601." },
            "rationale": { "type": "string", "description": "Why this change was made." },
            "eval_delta": {
              "type": "object",
              "additionalProperties": false,
              "description": "The evidence that justified the change.",
              "properties": {
                "suite": { "type": "string" },
                "before": { "type": "string" },
                "after": { "type": "string" }
              }
            },
            "permissions_delta": { "type": "string", "enum": ["none", "widened", "narrowed"], "description": "Capability change vs parent. 'widened' is human-gated, never auto-applied." },
            "parent_digest": { "type": "string", "description": "sha256:… of the parent version — chain integrity." }
          }
        }
      }
    },

    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "description": "Build origin + signature, so packages are verifiable supply-chain artifacts.",
      "properties": {
        "source": { "type": "string" },
        "commit": { "type": "string" },
        "digest": { "type": "string", "description": "sha256:… of the package artifact." },
        "signed": { "type": "boolean" }
      }
    }
  }
}
